size_t left(size_t& l)
{
return 2l;
}

size_t right (size_t& r)
{
return 2r+1;
}

template <typename T>
void max_heapify(std::vector<T>& A, size_t i)
{
size_t l = left(i);
size_t r = right(i);

if(l <=A.)

}
