#include <vector>

using namespace std;

template<typename T>
int linear_search(vector<T>& vec, int len, T target)
{

	for (int i = 0; i < len; ++i)
	{
		if (target == vec[i])
			return i;
	}
  
	return -1;
  
}
